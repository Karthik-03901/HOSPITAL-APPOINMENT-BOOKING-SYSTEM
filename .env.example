# Not read directly by this plain HTML project (no bundler/env loader) —
# copy these values into js/config.js instead. Kept here so the required
# variables are documented in one place.
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
